# Building Documentation

To build the documentation:

1. Install the documentation dependencies with `pip install -r docs/requirements.txt`.
2. Build the HTML site with `make html` on Unix-like systems or `make.bat html` on Windows.
3. Open `docs/build/html/index.html` in a browser.

The documentation source files live under `docs/source`.
